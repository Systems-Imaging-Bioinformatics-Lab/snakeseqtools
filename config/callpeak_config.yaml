#Enter treatment sample names
treatments:
  - GCLM_rep1
  - GCLM_rep2

#Provide controls if present, if not comment out these lines
#controls:
# - control1
# - control2...

#yes or no
control_present: "no"


#If calling broad peaks use --broad
broad_peak: ""

blacklist: "/scratch/ukarvind_root/ukarvind99/anashank/pipeline/ChIPseq_ATACseq_pipelines/data/references/blacklist/hg19-blacklist.v2.bed.gz"

macs_params: "-q 0.1 --keep-dup all --nomodel"
